# ── LLM 网关（必填）──
NEW_API_KEY=
NEW_API_URL=https://new-api.100xsoon.com

# ── Yarrow 身份（serenity bootstrap 自动写入；正式后端默认 api.yarrowlab.ai）──
YARROW_BASE_URL=https://api.yarrowlab.ai
YARROW_API_KEY=
YARROW_WALLET_ADDRESS=
# 仅一次性 SIWE bootstrap 用；长跑 daemon 应清空并只用 api key
YARROW_PRIVATE_KEY=

# ── 检索（可选；未配则 research 降级 RSS）──
SEARX_BASE_URL=https://api.yarrowlab.ai
SEARX_TOKEN=

# ── serenity 信念先验（默认值即评审定稿；实验期冻结，改 = 新实验段）──
# CORPUS_JSON_PATH=~/corpora/serenity-aleabitoreddit/data/aleabitoreddit_tweets.json
# DISTILL_MODEL=gpt-5.5
# GATE_MODEL=gpt-5.5
# PRIOR_MODEL=claude-opus-4-7
# PRIOR_DELTA_GRID=0.10,0.20,0.35
# PRIOR_ADJACENT_FACTOR=0.5
# PRIOR_SCALE=1.0
# EXPERIMENT_MIN_PAIRED=60

# ── 成本上限 ──
DAILY_LLM_COST_CAP_USD=40
